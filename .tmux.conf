# Split panes using logical visual keys: | and -
bind | split-window -h
bind - split-window -v
unbind '"'
unbind %

# Enable mouse mode (allows dragging pane sizes and clicking tabs)
set -g mouse on

# Design: Make the active pane border distinct
set -g pane-active-border-style fg=blue