---
- name: Download and dearmor HashiCorp GPG key safely
  shell: |
    set -o pipefail
    curl -fsSL https://apt.releases.hashicorp.com/gpg | gpg --dearmor -o /usr/share/keyrings/hashicorp-archive-keyring.gpg
  args:
    executable: /bin/bash
    creates: /usr/share/keyrings/hashicorp-archive-keyring.gpg
  become: true

- name: Add HashiCorp Repository using legacy-safe syntax
  apt_repository:
    repo: "deb [arch=amd64 signed-by=/usr/share/keyrings/hashicorp-archive-keyring.gpg] https://apt.releases.hashicorp.com noble main"
    state: present
    filename: hashicorp
  become: true

- name: Install Terraform CLI
  apt:
    name: terraform
    state: present
    update_cache: yes
  become: true

- name: Install AWS CLI utility via apt
  apt:
    name: awscli
    state: present
  become: true